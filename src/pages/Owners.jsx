import { Typography, Box } from '@mui/material';

const Owners = () => (
    <Box>
        <Typography variant="h4" fontWeight={800} mb={2}>
            Dashboard
        </Typography>
        <Typography variant="body1">Resumen general del sistema.</Typography>
    </Box>
);

export default Owners;
